[package]
name = "querypilot"
version = "0.1.0"
edition = "2021"
description = "Query Pilot agent CLI"
authors = ["Query Pilot"]
license = "MIT"

[[bin]]
name = "querypilot"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dirs = "5.0"
